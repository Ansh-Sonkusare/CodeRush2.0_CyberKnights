import type { Hono } from "hono";

// Stub — filled in when apps/service-orchestrator is implemented (Phase 9).
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type OrchestratorRoutes = Hono<any, any, any>;
