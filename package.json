{
  "name": "x402-sentinel",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "x402 Sentinel — Algorand agent payment router with policy guard",
  "scripts": {
    "dev": "concurrently -n providers,planner,orchestrator,gateway,web -c cyan,magenta,yellow,green,white \"pnpm --filter service-providers dev\" \"pnpm --filter service-planner dev\" \"pnpm --filter service-orchestrator dev\" \"pnpm --filter gateway dev\" \"pnpm --filter web dev\"",
    "build": "pnpm -r --filter=./packages/* build && pnpm -r --filter=./apps/* build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "legacy:providers": "tsx scripts/start-providers.ts",
    "legacy:trace-server": "tsx scripts/trace-server.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "concurrently": "^9.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@sentinel/config": "workspace:*",
    "zod": "^4.4.3"
  }
}
